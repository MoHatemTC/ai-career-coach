# Normalize all text files to LF in the repo (Git auto-detects binary)
* text=auto eol=lf

# Explicit LF for scripts and env files
*.sh text eol=lf
*.env text eol=lf
.env.* text eol=lf
Makefile text eol=lf
